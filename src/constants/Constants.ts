export const GRID_SIZE = 400; // Tamanho da malha (400px x 400px)
export const GRID_CENTER = GRID_SIZE / 2; // Centro da malha
export const SQUARE_SIZE = 50; // 1 cm = 50px
export const CIRCLE_RADIUS_PX = 75; // Raio do círculo central (em px)
export const MAX_SHOTS = 3;
